import * as React from 'react';
import * as styles from './Header.module.scss';

export const Header: React.FC = () => (
  <header className={styles.wrapper}>
    <h1>Jedrzej Lewandowski - Portfolio Page</h1>
  </header>
);
